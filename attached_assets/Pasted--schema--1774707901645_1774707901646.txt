从现在开始继续全程中文回复。

当前不做新功能开发，只做“管理员群审批链路端到端验收 + 小范围收口修复”。

禁止扩展到自动交易、自动发币、自动买入。
禁止改数据库 schema。
禁止改前端。
禁止 push。

本轮目标只有 3 件事：

====================
第一件事：把 TELEGRAM_ADMIN_CHAT_ID 链路彻底跑通
====================

请按下面步骤执行，并输出中文结果：

1. 保持当前服务可启动（即使 ADMIN_CHAT_ID 不合法，也不要退出）
2. 确保 /chatid 命令可用
3. 明确说明：
   - 私聊机器人时 /chatid 返回什么
   - 群组里发送 /chatid 返回什么
   - 管理群最终应该填哪个值到 TELEGRAM_ADMIN_CHAT_ID
4. 输出一段非常短的中文操作说明，给用户照着做：
   - 把机器人拉进群
   - 给管理员权限（如果需要）
   - 在群里发送 /chatid
   - 复制机器人返回的负数 chat id
   - 填入 Replit Secrets
   - 重启服务

====================
第二件事：做一次端到端验收
====================

在用户把正确的 TELEGRAM_ADMIN_CHAT_ID 填好之前，先准备好验收代码和日志。
填好以后，按下面顺序做完整验收：

A. 启动日志检查
必须输出：
- Telegram 机器人已启动（手动长轮询）
- ADMIN_CHAT_ID 当前状态（合法/不合法）
- 审批发送功能是否启用

B. 自检命令检查
执行 /chatid，确认能返回当前 chat id

C. 发送链路检查
增加一个“仅测试用”的最小内部触发方式（二选一）：
1. 增加一个安全的测试函数，发送一条测试审批消息到 ADMIN_CHAT_ID
或
2. 增加一个仅本地可调用的测试命令，构造一条假 candidate 并走 sendCandidateForApproval()

要求：
- 不依赖真实 Twitter 抓取
- 不依赖真实链上私钥
- 不新增复杂接口
- 只用于本轮验收

D. 审批按钮检查
验证消息发出后，inline keyboard 按钮存在
点击任意一个按钮后：
- 有中文日志
- 有 audit log
- 不报错

====================
第三件事：做两个最小收口修复
====================

【修复 1：配置状态显式化】
当前 TELEGRAM_ADMIN_CHAT_ID 不合法时只是 warning，不够清晰。
请改成：

- TELEGRAM_BOT_TOKEN 缺失/非法：启动失败退出
- TELEGRAM_ADMIN_CHAT_ID 缺失或格式不合法：服务可启动，但必须明确打印中文状态：
  “管理员群未配置正确，审批消息发送功能当前已禁用。请先通过 /chatid 获取正确群组 ID。”

并且：
- 调度器不要每轮都疯狂尝试发消息刷错误
- 当 ADMIN_CHAT_ID 不合法时，本轮发送直接跳过，并打印一次结构化中文日志
- 不要重复刷屏

【修复 2：风控规则说明补齐】
你本轮把 warfare / wartime 加入了风险词。
请不要把它写成“边界匹配修复的一部分”，而要在日志或注释里明确写明：
“新增高风险关键词：warfare, wartime”
这是规则补充，不是 bug 本身。

====================
本轮允许修改的文件（仅限）
====================

- artifacts/api-server/src/services/telegramBot.ts
- artifacts/api-server/src/jobs/scheduler.ts
- artifacts/api-server/src/lib/config.ts
- 如确有必要，可最小修改：
  - artifacts/api-server/src/index.ts

不要修改：
- riskEngine.ts（除非只补注释/日志，不改逻辑）
- buildCandidates.ts
- adapters
- 前端
- 数据库 schema

====================
改完后的输出要求
====================

不要空泛总结，严格按下面顺序输出：

1. 实际修改了哪些文件
2. 每个文件改了什么
3. 用户现在具体要做什么（只保留 5 步以内）
4. 贴原始输出：
   - git status --short
   - git diff --stat
   - 关键启动日志
   - /chatid 测试结果
   - 测试审批消息发送结果
   - 按钮点击后的日志结果
5. 最后明确写：
   - 是否已 commit：是/否
   - 是否已 push：是/否

注意：
- 全程中文
- 不要扩大修改范围
- 不要顺手新增功能
- 不要 push